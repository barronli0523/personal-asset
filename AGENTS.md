# AI 维护说明

本仓库是公开的 Obsidian 项目经验整理模板。先读 `30_System/purpose.md`、`schema.md`、`taxonomy.md`、`linking-rules.md`、`index.md`，再操作。

## 工作区与权限

开始时确认原始资料目录、知识库目录和交付目录。原始项目默认只读，知识整理不等于获得修改源码、运行项目或发布的权限。没有 Git 时不自行初始化；不擅自删除、移动原件、修改秘密、安装依赖或公开发布。

- `00_Inbox`：人的原始输入，AI 只读。
- `01_Sources`：证据仅新增；需要更正时新建更正条目。
- `10_Work`：经用户委托提炼工作资产。已有内容只补事实、链接和使用记录；结论变化先提交 review，不抹去失败历史。
- `20_Thinking`：代表使用者自己的判断；仅明确授权后创建或修改观点。
- `30_System`：规则、模板修改须获得授权；日志只追加，待决事项进入 review。
- `examples`：公开的虚构示例，不是实际项目证据。
- 新条目默认 `draft`；只有使用者确认后才设为 `verified`。

## 操作流程

1. 查重：先查索引和相关条目，不全量加载所有记录。
2. Source：固定来源或建立指针；落盘前分别检查凭据、个人信息、第三方商业信息。隐私未知按受限处理，敏感原文留在原处，优先使用最小化指针。
3. Extract：提取事实、失败尝试、决策原因、环境问题、冲突和未知项。
4. Evaluate：先查重并判断证据、复用价值和推荐去向。外部项目复盘产生的候选一律先进入 `30_System/review/`，不得直接写成正式资产。
5. Review：一候选一条 review，记录来源、证据、解决的问题、复用场景、适用范围、例外、声明类型、隐私、相似资产、推荐目的地和需要人工决定的事项。只有使用者批准当前候选版本、具体目的地和动作后才进入 Asset。
6. Asset：按模板写成具体经验或标准做法，保留来源、适用条件、例外和验收方式。`20_Thinking` 还需要使用者明确确认具体观点。
7. 更新索引，追加 `30_System/logs/YYYY-MM-DD.md`。报告新增资产；为零时说明为何不需要新建。

数字、根因、成本和节省不能猜。允许有依据且明确标注的估算：必须记录输入来源、方法或公式、假设和误差/范围；无法量化误差时说明原因。推断必须列出事实前提和未验证部分。个人判断标明判断者。证据不足写 unknown / 无法确认，不把推断、估算或个人判断冒充实测事实。未运行不能写测试通过；纸面演练不能写真实收益。没有失败记录就写无法确认，不为满足模板编造失败。

## 复用验收

先判断功能必要性，不自行发明评分权重、阈值或额外架构。对清单每一步和检查项，说明落成什么动作或为何不适用。记录实际执行与未执行项、来源和需要修订之处；不因命中关键词而强行套用。

修改后检查 YAML 格式、必填字段、标签、链接和索引；使用目标 Obsidian 检查阅读效果。检查范围和未验证部分如实报告。
